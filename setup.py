from setuptools import setup, find_packages

setup(
    name='document_portal',
    version='0.1.0',
    description='Document processing and Management Portal',
    author='Shiraj S',
    packages=find_packages()
)